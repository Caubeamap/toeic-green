# Front-End

Next.js front-end for TOEIC Green.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run build
```

From the repository root, the same commands are proxied through `npm --prefix
front-end`.
